const Users = () => {
  return (
    <section>
      <div className="p-8">
        <h2 className="font-semibold">Select a user to see a list of todos</h2>
      </div>
    </section>
  );
};

export default Users;
